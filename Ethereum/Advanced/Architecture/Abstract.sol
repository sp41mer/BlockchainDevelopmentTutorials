// Указываем версию Solidity

// Объявляем абстрактный контракт

    // Объявляем функцию, реализация которой будет в другом контракте

// Объявляем основной контракт, который является наследником абстрактного контракта

    // Реализуем функцию, которая была объявлена в абстрактном контракте